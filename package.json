{
  "name": "cricket-stats-root",
  "version": "1.0.0",
  "description": "Unified build for Cricket Stats MERN app on Render",
  "scripts": {
    "install-frontend": "cd frontend && npm install",
    "install-backend": "cd backend && npm install",
    "build-frontend": "cd frontend && npm run build",
    "render-build": "npm run install-frontend && npm run build-frontend && npm run install-backend",
    "start": "cd backend && node server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
